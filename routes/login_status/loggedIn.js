// module.exports = function (req, res, next) {
//   if (req.isAuthenticated() === true) {
//   req.flash('error', 'You are already logged in.')
//   return res.redirect('/users/'+ req.user.id)}
//   else return next() //to change the redirect address
// }
